apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Shared Jobs used by both staging and production overlays.
# Referenced via ../../jobs from each overlay.

resources:
  - db-migrate.yaml
